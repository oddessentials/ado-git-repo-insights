# The Promise

Every CI quality check has a local equivalent. No surprises.

Sorted by historical failure frequency (most failed first). "Next Push" and "Next PR" columns predict whether this check will pass locally but fail in CI on the next push/PR from this branch.

| # | Check | Why It Exists | CI Failures | What Changed This PR | Why It Will Be Caught Locally | Why It Was Desynced and Why That's Fixed | Next Push | Next PR |
|---|-------|--------------|:-----------:|---------------------|-------------------------------|----------------------------------------|:---------:|:-------:|
| 1 | test (Python, 9 matrix) | Validates Python code across 3 OS x 3 Python versions | 359 | No change to Python code | `pytest tests/` in pre-push runs on baseline Python. | 359 failures are the same ~40 bugs repeated 9x across the OS/Python matrix. Local runs baseline Python only — cross-platform issues (Windows paths, macOS timing) are CI-only by nature. This is an accepted tradeoff: full matrix takes 15+ min locally. | No | No |
| 2 | extension-tests (tsc) | Catches TypeScript type errors (unused vars, type mismatches, strict null) | 29 | Added `run_extension_typecheck()` to pre-commit hook | `tsc --noEmit` now runs in pre-commit when TS files are staged. Added in 5d18b31 after 4 prior escapes. | **The most desynced check in the project.** `tsc --noEmit` existed in CI since the project started but was NEVER in any local hook. esbuild (pre-commit) and ts-jest (tests) both skip type checking. 4 separate incidents (88ed3b7, 7264576, 3247874, PR #207) escaped before we added tsc to pre-commit. Fixed permanently in this PR. | No | No |
| 3 | build-extension (tsc+esbuild) | Validates extension compiles and bundles | 17 | tsc errors fixed; CI now depends on extension-tests for type check | `build:check` + `build:ui` in pre-push preflight. | 17 failures include VSIX packaging, task staging, and tsc linking errors. Local ran esbuild only (no tsc). Fixed: tsc now in pre-commit (check #2), and CI `build-extension` depends on `extension-tests` which gates tsc — eliminating redundant tsc runs. | No | No |
| 4 | ui-bundle-sync | Ensures extension dist, ui_bundle, and docs are in sync | 12 | Typeahead component added; artifact sync runs on every UI change | `run_managed_artifacts("sync")` in pre-commit rebuilds and syncs all bundles when TS files are staged. | All 12 failures predated the managed-artifact pipeline. Before commit 3247874, UI builds were manual — developers forgot to rebuild before pushing. Now the pre-commit hook detects TS changes and auto-syncs all artifacts. | No | No |
| 5 | fresh-clone-verify | Proves builds work from clean state without cache | 12 | CI job now uses `build:ui` (esbuild only) instead of `build` (tsc+esbuild); tsc gated by extension-tests | CI-only by design. Cannot simulate fresh clone locally. | Not desynced — intentionally CI-only. Tests lockfile determinism and cache-free builds. 12 failures were from lockfile regeneration and stale cache. Local preflight assumes clean pnpm state (reasonable — developers work on checked-out repos). | No | No |
| 6 | suppression-audit | Tracks lint suppression growth; blocks increases without approval | 11 | Made local audit strict (removed `--allow-pending-approval` for non-refactor branches) | `audit-suppressions.py --diff` in pre-push preflight now runs same strict mode as CI. | **The second most desynced check.** Local ran in warning-only mode (`--allow-pending-approval`) while CI blocked strictly. 11 CI failures where developers saw warnings locally but pushed anyway. Fixed in this PR (commit db5b04b) — local is now equally strict. Intentional asymmetry remains: local compares branch baseline, CI compares main baseline. `SUPPRESSION-INCREASE-APPROVED` is the policy gate. | No | No |
| 7 | Run Demo Tests | Validates demo dataset contract and rendering | 5 | No change to demo data | `pytest tests/demo/` in pre-push preflight. Identical pytest command. | 5 failures during demo pipeline buildout (branches 031, 037). Demo tests weren't in the preflight initially. Added in commit 3247874; zero failures since. | No | No |
| 8 | baseline-integrity | Validates perf/suppression baselines are internally consistent | 3 | No change | `check-baseline-integrity.js` runs in pre-push. Identical Node script. | 3 failures from corrupted baseline files during rapid iteration. Now self-healing with consistent baseline format. Same Node script runs locally and in CI. | No | No |
| 9 | line-ending-guard | Prevents CRLF in Unix-executed files (.husky, .sh, scripts) | 2 | No change | `run_crlf_guard()` in pre-push scans all target directories. Same targets as CI. | 2 failures early in project history on Windows development before the CRLF guard existed. Added to pre-push in commit 3247874. Zero failures since. | No | No |
| 10 | extension-tests (smoke) | Validates real browser rendering of dashboard via Playwright | 2 | Updated selectors from `locator("option")` to `locator('[role="option"]')` + added `data-testid` attributes | `pnpm run test:smoke` in pre-push preflight. | 2 failures both from this PR — smoke tests used DOM tag selectors (`<option>`) that broke when we replaced `<select>` with typeahead `<div>`. The preflight includes smoke tests but they weren't run locally before the first push. Now fixed with stable `[role="option"]` + `data-testid` selectors that survive DOM refactors. | No | No |
| 11 | Regenerate Demo Data | Validates demo generation contract and byte-identical output after full rebuild | 2 | No change to demo generation | `validate_demo_generation_contract.py` in pre-push preflight. Contract validation only; full rebuild + byte-identical diff is CI-only (expensive). | 2 failures during demo pipeline development (branches 031, 037). Contract validation wasn't in preflight initially. Added in commit 3247874. CI runs full `build-demo-dataset.py` then `git diff docs/`; local validates the generation contract only — catches schema and config drift without expensive data rebuild. | No | No |
| 12 | pnpm-lockfile-guard | Prevents `package-lock.json` from entering the repo (pnpm-only policy) | 1 | No change | `run_pnpm_lockfile_guard()` in pre-commit scans staged files for package-lock.json. Identical logic to CI. | 1 failure early in project history before the pre-commit guard existed. Guard added in commit 3247874; zero failures since. | No | No |
| 13 | npm-command-guard | Prevents `npm ci`/`npm install` in scripts (pnpm-only policy) | 1 | No change | `run_npm_command_guard()` in pre-commit scans staged files with identical allowlist (tfx-cli). | 1 failure before the guard was added to pre-commit. Now runs on every commit with the same allowlist as CI. | No | No |
| 14 | mypy | Catches Python type errors (strict mode) | 1 | No change | `mypy src/` runs in pre-push preflight. Identical config from pyproject.toml. | 1 failure from ML module type errors (commit 8ed7193). mypy was initially CI-only; added to pre-push preflight in commit 3247874. Same strict config now runs locally. | No | No |
| 15 | Verify Byte-Identical Output | Ensures generated artifacts match source | 1 | Bundle rebuilt with typeahead + data-testid changes; verified in sync | `manage_generated_artifacts.py verify --scope all` in pre-push preflight. | 1 failure from stale docs artifact. Local verify was added to preflight in commit 3247874. CI does a narrower `git diff docs/` check; local does broader artifact parity. Both catch drift. | No | No |
| 16 | threshold-change-guard | Prevents accidental coverage threshold changes | 1 | No change | `check_threshold_changes.py` in pre-push preflight. Compares against main branch. | 1 failure from threshold update without `[threshold-update]` marker. Guard was CI-only initially; added to preflight in commit 3247874. | No | No |
| 17 | ci-guards | Validates CI config integrity (composite actions, packageManager field) | 0 | No change | Pre-commit framework runs formatting and structural checks on every commit. CI-config validation is GitHub-specific and cannot fail from code changes. | Never desynced. GitHub-context checks (composite action versions, packageManager field) are inherently CI-only. Zero failures confirms no local gate needed. | No | No |
| 18 | pagination-token-guard | Enforces centralized pagination helper usage (security) | 0 | No change | `run_pagination_token_guard()` in pre-commit scans staged files for direct continuationToken usage. | Never desynced. Guard was added to pre-commit and CI simultaneously. | No | No |
| 19 | extension-tests (ESLint) | Catches lint violations, security patterns, code style | 0 | No change | `run_extension_lint()` in pre-commit runs `eslint ui/ --max-warnings=0`. Same command as CI. | Was briefly desynced — ESLint was CI-only until commit 7264576 added it to pre-commit after a non-null assertion error escaped. Zero failures since. | No | No |
| 20 | extension-tests (Jest) | Validates all extension unit and integration tests | 0 | 152 new test cases added across 20 test files (1948 total); regression tests for 3 PR #207 bugs | `jest --ci --coverage` in pre-push preflight. Identical command, same reporters. | Never desynced. Jest has been in the pre-push preflight since the preflight was created. | No | No |
| 21 | extension-tests (type-tests) | Validates compile-time type regression tests | 0 | No change | `pnpm run test:types` in pre-push preflight. Identical tsconfig.type-tests.json. | Never desynced. Added to preflight and CI simultaneously. | No | No |
| 22 | build (Python) | Validates Python package builds successfully | 0 | No change to Python packaging | `python -m build --sdist` in pre-push preflight. | Never desynced. Local builds sdist; CI builds both sdist+wheel. Zero wheel-only failures in history confirms sdist is sufficient. | No | No |
| 23 | build-extension (VSIX) | Validates marketplace package builds | 0 | No change | `test:vsix` artifact inspection in pre-push preflight. | VSIX packaging requires tfx-cli (CI-only by design). Local runs `test:vsix` which inspects the artifact structure. Zero failures from VSIX-only issues confirms inspection is sufficient. | No | No |
| 24 | build-extension (task tests) | Validates ADO pipeline task JavaScript | 0 | No change | `node index.test.js` in pre-push preflight. Identical command. | Never desynced. Same Node command locally and in CI. | No | No |
| 25 | build-extension (task input) | Validates task.json input definitions | 0 | No change | `ts-node validate-task-inputs.ts` in pre-push preflight. Identical command. | Never desynced. Added to both simultaneously. | No | No |
| 26 | Check Directory Size | Prevents docs/ from exceeding 50 MB | 0 | No change | 50 MB limit check in pre-push preflight. Identical logic. | Never desynced. Added to both simultaneously. | No | No |
| 27 | test count (Python) | Ensures pytest discovers minimum expected tests (min=312) | 0 | No change to Python tests | `validate-test-results.py` in pre-push preflight. Same minimum threshold as CI. | Never desynced. Added to preflight and CI simultaneously. | No | No |
| 28 | test count (Extension) | Ensures Jest discovers minimum expected tests (min=632) | 0 | 152 new test cases added; count now 1948 (well above 632 minimum) | `validate-test-results.py` in pre-push preflight. Same minimum and max-skips thresholds. | Never desynced. Added to preflight and CI simultaneously. | No | No |
| 29 | patch coverage parity | Ensures new code has adequate test coverage | 0 | Coverage gaps closed: typeahead, tooltip-manager, data-availability, summary-cards, classifier | `check_patch_coverage.py` in pre-push preflight. Compares Python + TypeScript coverage against main. | Never desynced. Coverage parity has always run in preflight. | No | No |
| 30 | pandas version policy | Ensures pandas major version matches Python version | 0 | No change | Runtime version check in pre-push preflight. Identical logic. | Never desynced. Inline check in preflight matches CI. | No | No |
| 31 | secret-scan (gitleaks) | Detects committed secrets via full git history scan | 0 | No change | CI-only by design. Requires full git history scan via external tool. | Not desynced — intentionally CI-only. Gitleaks requires full git history traversal. `detect-private-key` pre-commit hook provides partial local coverage for common patterns. | No | No |
| 32 | scalability-tests | Validates dashboard rendering at enterprise scale (10K PRs, 156 weeks) | 0 | No change | CI-only by design. Run locally on demand via `pnpm run test:scalability`. | Not desynced — intentionally CI-only. Synthetic data generation (10K PRs, 156 weeks, 200 reviewers) is expensive. Zero failures confirms the scalability invariants are stable. | No | No |
