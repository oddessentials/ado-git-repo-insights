#!/bin/sh
# Capture-only stage. entire's installed hook wrapper (.husky/_/post-commit) owns
# session capture; this tracked hook intentionally does nothing, to avoid
# double-recording. Kept so .husky/_ has a self-contained dispatcher to chain to.
